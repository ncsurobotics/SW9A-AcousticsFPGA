n = 0:1:15
x = ones(1,16)
pointsize = 16
xpad = zeros(1,pointsize );
xpad(1:16) = x;
dm = dftmtx(pointsize)
X = xpad * dm;
f = fft(x)
        yyaxis left
	    plot(k,imag(oracle_var(i,k+128)), 'LineWidth', 2);
        ylabel('Power');
    
        yyaxis right
	    plot(k,verilog_data(i+1,k+128), 'LineWidth', 2);
        ylabel('Power');
    
    
	    xlabel('Frequency index');
    